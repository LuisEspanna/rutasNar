Esta carpeta solo debe contener todo lo relacionado con postgres

//--------------------------------------------------------------------------------------
Backup:
Solo se guarda los datos que se almacenen en la base de datos en formato plano SQL.

//---------------------------------------------------------------------------------------
Queries:
Se usa para guardar las consultas SQL como creacion de tablas, modificacion,
creacion de llaves primarias, relaciones.

//---------------------------------------------------------------------------------------
Schemes:
Se guardarán los esquemas SQL de la relaciones en tablas, ya sea formato imagen o los
.xml.

En caso de usar archivos .xml especificar el programa en el cual fue realizado.


NOTA:
Tener ciudado con el editor y poner en el git ignore las carpetas que no sean parte del proyecto
